{
    "Java": {
        "variable": "${type} ${name} = ${value};\n",
        "variables": {
            "Int": "int"
        }
    }, 

    "Swift": {
        "variable": "var ${name} : ${type} = ${value}; \n",
        "variables": {
            
        }
    }
}